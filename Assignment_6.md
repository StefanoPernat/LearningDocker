# Assignment VI: Bind Mounts

Let's see how bind mounts work

- `docker container run -p 80:4000 -v $(pwd):/site bretfisher/jekyll-serve`
- Trying to edit files
- Refreshing browser
- I can see my edits